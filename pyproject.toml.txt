[project]
name = "identity-geometry"
version = "0.1.0"
description = "Fisher-Rao information geometry + rational inattention novelty scoring"
requires-python = ">=3.9"
license = {text = "Apache-2.0"}
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
    "numpy>=1.24",
    "scipy>=1.10"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
